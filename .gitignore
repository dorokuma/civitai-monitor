# Dependencies
__pycache__/
*.pyc
.env
venv/
.venv/

# Config (copy from config.yaml.example — never commit credentials)
config.yaml

# Runtime data (auto-generated)
seen_ids/
downloads/
civitai_cookies.txt
*.log

# OS junk
.DS_Store
Thumbs.db
.commit_msg
*.log
.monitor.lock
*.bak
*.env
monitor_status.json
.last_scheduled_scan
# systemd units (if user wants to manage separately)
